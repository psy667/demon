{
  "name": "demon",
  "module": "index.js",
  "type": "module",
  "scripts": {
    "compile": "bun build index.ts --compile --outfile ./bin/demon --minify --target=bun-darwin-arm64",
    "format": "bun x prettier ./ -w"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
